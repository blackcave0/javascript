//*_________________________Associative Arrays_____________________
/*
    ? While many programming languages support arrays with named indexes (Test instead of numbers), called    __associative arrays__, JavaScript does not. 

    ? However, you still can use the named __array__ syntax, which will produce an obect. 
 */
